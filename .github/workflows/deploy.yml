name: Deploy Student Registration Form

on:
  push:
    branches:
      - main

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  deploy:
    runs-on: ubuntu-latest

    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload Student Registration Form
        uses: actions/upload-pages-artifact@v4
        with:
          path: './MINI_PROJECTS/Student_Registration_Form'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
